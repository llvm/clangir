// RUN: %clang_cc1 -triple x86_64-unknown-linux-gnu -fenable-clangir -emit-llvm %s -o %t.cir
// RUN: FileCheck --input-file=%t.cir %s
// XFAIL: *

module {
  func.func @foo() {
    cir.return
  }
}

// CHECK: define void @foo()
// CHECK-NEXT: ret void,
// CHECK-NEXT: }
